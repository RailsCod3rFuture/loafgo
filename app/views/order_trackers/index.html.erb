<p id="notice"><%= notice %></p>

<h1>Order Trackers</h1>

<table>
  <thead>
  <tr>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @order_trackers.each do |order_tracker| %>
    <tr>
      <td><%= link_to 'Show', order_tracker %></td>
      <td><%= link_to 'Edit', edit_order_tracker_path(order_tracker) %></td>
      <td><%= link_to 'Destroy', order_tracker, method: :delete, data: {confirm: 'Are you sure?'} %></td>
    </tr>
  <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Order Tracker', new_order_tracker_path %>
